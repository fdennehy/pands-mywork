# show hhow you would use a configuration file
# this is the file you would save to Github, 
# and would keep the config file locally (sensitive info)

import config as cfg

password = cfg.gmail['password']

print(password)

